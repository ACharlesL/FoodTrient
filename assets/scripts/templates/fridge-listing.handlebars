{{#each fridges as |fridge|}}

  <!--
  {{log fridge}}
   {{> partial}}
  Don't uncomment yet
  store.user.id === data.fridges.fridges.user_id-->

  <div>
    <h1 data-id="{{fridge.id}}"> <small> item: {{fridge.id}}</small></h1>
    <section data-id="{{fridge.id}}"
    <p>{{#if fridge.ingredient_id}}
      {{fridge.ingredient_id}}
    {{else}}
      No author provided.
    {{/if}}
    </p>
    <button type="submit" class="btn btn-default delete">Remove</button>
    <button type="submit" class="btn btn-default edit" >Edit</button>
    </section>

  </div>

{{/each}}
