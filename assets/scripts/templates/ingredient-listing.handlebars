{{#each ingredients as |ingredient|}}
  {{log ingredient}}
  <!-- {{> partial}}
  Don't uncomment yet -->

  <div>
    <h1 data-id="{{ingredient.id}}"> <small>{{ingredient.name}}</small></h1>
    <section data-id="{{ingredient.id}}"
    <p>{{#if ingredient.id}}
      {{ingredient.id}}
    {{else}}
      No author provided.
    {{/if}}
    </p>
    <button type="submit" class="btn btn-default">Remove</button>
    </section>

  </div>

{{/each}}
