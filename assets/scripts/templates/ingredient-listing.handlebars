{{#each ingredients as |ingredient|}}
  {{log ingredient}}
  <!-- {{> partial}}
  Don't uncomment yet -->

  <div>
    <h1 data-id="{{ingredient.id}}"> <small>Title: {{ingredient.name}}</small></h1>
    <section data-id="{{ingredient.id}}"
    <p>{{#if ingredient.unit}}
      {{ingredient.unit}}
    {{else}}
      No author provided.
    {{/if}}
    </p>
    <button type="submit" class="btn btn-default delete">Remove</button>
    <button type="submit" class="btn btn-default edit" >Edit</button>
    </section>

  </div>

{{/each}}
